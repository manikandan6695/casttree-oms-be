export enum EPricingScheme {
  tier = "Tier",
  volume = "Volume",
  unit = "Unit",
  package = "Package",
}

export const ESPricingScheme = [
  EPricingScheme.tier,
  EPricingScheme.unit,
  EPricingScheme.volume,
  EPricingScheme.package,
];
